{
  "name": "volleyball-pwa-tools",
  "version": "1.0.0",
  "description": "Small helper scripts for optimizing icons in the volleyball PWA",
  "scripts": {
    "build": "rm -rf www || true && mkdir -p www && cp -R index.html manifest.webmanifest assets www/ || true",
    "optimize-icons": "node tools/optimize-icons.js"
  },
  "dependencies": {
    "sharp": "^0.32.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
